target-version = "py310"

[lint]
# Ruff 0.16 changed its no-config default from the traditional Pyflakes/
# pycodestyle rules to all rules. Keep CI deterministic and focused on the
# established project baseline instead of silently inheriting future defaults.
select = ["E4", "E7", "E9", "F"]
